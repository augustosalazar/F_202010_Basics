// generate code with with st

import 'package:flutter/material.dart';

class BasicStateless extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}